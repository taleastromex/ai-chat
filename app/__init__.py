"""AI-FABLE application package.

A thin FastAPI proxy + web UI in front of Ollama, serving the Gemma-4 12B
coder model via a GGUF quantization (see README for why Ollama is used
instead of the `transformers` library).
"""
