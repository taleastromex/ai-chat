# AI-FABLE runtime dependencies — used by the Docker image.
# No torch / transformers here: Ollama does the actual inference in its own
# process using the GGUF build of the model (see README for why).

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9      # for the image upload endpoint
aiofiles>=23.2.1             # required by FastAPI StaticFiles
httpx>=0.27.0                # HTTP client to talk to Ollama
requests>=2.31.0             # fetch remote image URLs

python-dotenv>=1.0.0
loguru>=0.7.0
